<%= form_with(model: artist, local: true) do |form| %>
  <% if artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :artist_name %>
  </div>

  <div class="field">
    <%= form.label :dob %>
    <%= form.date_select :dob, id: :artist_dob %>
  </div>

  <div class="field">
    <%= form.label :placeOfBirth %>
    <%= form.text_field :placeOfBirth, id: :artist_placeOfBirth %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state, id: :artist_state %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :artist_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
